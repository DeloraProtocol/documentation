---
title: "Select Widget Variants"
description: "Package exports and integration patterns available in the Delora widget."
"og:image": "https://docs.delora.build/images/OG.png"
---

There are two practical integration patterns for the main widget.

- `TradeWidget`
- `TradeWidgetWalletProvider`

| Variant | Use when | Notes |
| --- | --- | --- |
| `TradeWidget` | You want the full trading widget with the built-in wallet flow | Best default for standalone embeds |
| `TradeWidget` + `TradeWidgetWalletProvider` | Your app already owns wallet connection and providers | Lets the widget reuse host-managed wallets |

## 1. `TradeWidget`

Use `TradeWidget` when you want the complete Delora trading experience, including token selection, network selection, quote loading, slippage control, wallet connection, and transaction execution.

```tsx
import "@deloraprotocol/widget/styles.css";
import { TradeWidget } from "@deloraprotocol/widget";

<TradeWidget
  theme="dark"
  config={{}}
/>;
```

## 2. `TradeWidget` with external wallet management

Use this pattern when the host app already manages wallet state and providers.

```tsx
import "@deloraprotocol/widget/styles.css";
import {
  TradeWidget,
  TradeWidgetWalletProvider,
} from "@deloraprotocol/widget";

<TradeWidgetWalletProvider
  value={{
    origin: {
      namespace: connected ? "EVM" : null,
      address: connectedAddress ?? null,
      walletName: "My App Wallet",
      evmProvider,
      connect: async () => {
        await openWalletModal();
        return true;
      },
      disconnect: async () => {
        await disconnectWallet();
      },
    },
  }}
>
  <TradeWidget config={{}} />
</TradeWidgetWalletProvider>;
```

You can manage `origin`, `destination`, or both sides. If one side is omitted, that side falls back to the widget's built-in wallet flow.
